import express from "express";
const router = express.Router();

// placeholder for slicing logic
router.post("/", async (req, res) => {
  res.json({ status: "FFmpeg Worker Placeholder" });
});

export default router;

